services:
  data-viewer:
    build: .
    ports:
      - "8081:80"
    environment:
      - API_URL=${API_URL:-https://zebra-api.highvelocitynetworking.com}
      - API_KEY=${API_KEY:-}
    restart: unless-stopped
